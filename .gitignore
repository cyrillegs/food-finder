node_modules/
.env
.env.local
.env.*.local

# build outputs
dist/
build/
.next/
out/
*.tsbuildinfo

# Playwright's own run artifacts (screenshots, traces) - not source
apps/web/test-results/
apps/web/playwright-report/

# prisma generated client (if generated into a custom output dir)
apps/api/src/generated/

# logs
npm-debug.log*
*.log

# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# private planning material - not for the portfolio/public copy of this repo,
# and not part of the take-home submission. Whole folder, so nothing new
# added here later needs a matching gitignore entry - if something in docs/
# genuinely needs to ship (has happened before: test-accounts.md), it needs
# an explicit `git add -f`, which is a deliberate, visible choice rather
# than an accident.
docs/
_move_log.txt

# local Claude Code tooling config (project-scoped skills, etc.) - dev
# tooling for working on this repo, not part of the submission itself.
.claude/
